{
	"name": "my-project",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "pnpm --parallel run dev",
		"build": "pnpm -r run build",
		"lint": "pnpm --parallel run lint",
		"use": "alien-start use"
	},
	"devDependencies": {
		"@types/node": "latest",
		"alien-start": "latest",
		"prettier": "latest",
		"typescript": "latest"
	}
}
